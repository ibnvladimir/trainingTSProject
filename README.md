Compiler installation
====
You need to have been installed typeScript compiler. 
`npm i -g typescript`.

For compile
===
Use `tsc index.ts` to compile intex.ts file to index.js.
Or any other `.ts` filename to compile it into `.js`

Configure file
===
Use `tsc --init` to create tsconfig.json